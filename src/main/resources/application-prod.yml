spring:
  application:
    name: user-center
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/usercenter
  session:
    timeout: 86400
server:
  address: 0.0.0.0
# 线上把一些公共的配置就不需要重复定义了